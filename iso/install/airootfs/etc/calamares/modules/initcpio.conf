# Without this file Calamares uses its upstream default, kernel: linux, and runs
# `mkinitcpio -p linux` in the target — a preset for a kernel Arch Colony does not
# ship (ADR-0004). The module then fails and the installation stops before a
# bootloader is ever written.
---
kernel: "linux-hardened"
